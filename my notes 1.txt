
episode 1 : 
Part-01
create basic html file for hellow world program
create basic js file for hellow world program
installation of CDN link form the website , inject them in html file

Part-02 : @30:55
we will just add h1 tag in div id=root

<div id="root"></div>
<script>
const heading =
React.createElement("h1", {id:"heading"}, "Hello world from React");
const root =
ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);

[.createElement takes 3 arguments -
1.h1 tag is the name of element , 
2.empty obj - it is used to give attributes to the class,
3."..."what are you going to keep in the obj
now we need to tell react that we need to put this element 
inside the div id="root"

creacting an element is a job of react library i.e .createElement
but
creating a root and rendering it inside the div is a job of reactDOM]

Part-03 : @40:50
injected css file in this file we can use that id used in {}

Part-04 : @53:37
Created mind bogling nested structures inside react

for ex if we need something like this:
<div id="parent">
 <div id="child">
   <h1>i m h1 tag</h1>
   <h2>i m h2 tag</h2>
 </div>
</div>

then in react we do:

const parent = React.createElement(
"div",
{id: "parent"},
React.createElement("div", {id: "child"}, [
React.createElement("h1", {}, "i am an h1 tag"),
React.createElement("h2", {},  "i am an h1 tag"),
])
);
console.log(parent);

nested children who r siblings are to be added int an array[....,...] 
now since this all looks even worse we wil use somethign called as jsx

Part-05 : @01:08:06
--------------------

episode 2 : 
Part-01
git and github commit , push etc,
what r bundlers - we are installing parcel as bundler
parcel: 
dev dep , version abt parcel , package lock .json
how to install npm
there r 2 tyes of dependencies that we install
1-dev dependencies-used in development of an app 
and
2-normal dependencies - used in production also
@55.02 gitignore

Part-02 @1:05
installation and everythhing about Parcel
--------------------

episode 3 :
Part-01
Project entry point

Part-02

Part-03
what si JSX? ITS SYNTAX
Babel - it is a library
JSX => Babel transpiles it to React.creatteElement => ReactElement-JS Object=> HTMLElement(render)
instalaltion of extentions like 
after using jsx we donot need
Prettier , bracket collorisor , Better comment
 Part-04 @1:04
functional compo , class based components
Component composition
PArt-05 @1:26
u can compose js in react tage anything inside anything
site sidescripting
--------------------

episode 4 :
Part-01
made a layout - jotted down what all we need in our app
made a AppLayout coponent
css: @23:08
Body @30:00
inline styling - Styleobj in restaurantcard @40
restaurantcard @50:10

Part-02 @59:21
how can we make different cards dynamic
Props @1:00:10 "thye r simply arguments to the function" 
like some properties thT WE CAN PASS TO THE COMPONENT
IT TAKEs all the properties and wraps it inside an object

Destructuring @1:05 
const RestaurantCard = (props) =>{
const { resName, Cuisine} = props;

Configdriven UI @1:14....
in websites